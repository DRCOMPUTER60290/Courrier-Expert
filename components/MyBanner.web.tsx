import React from 'react';
import { View } from 'react-native';

// Web version of MyBanner - returns empty view since AdMob is not supported on web
export default function MyBanner() {
  return <View />;
}